
def fib(x: Int): Int := if x ≤ 1 then x else fib(x - 1) + fib(x - 2)

def main(): Unit := {
    let a: Int = 10;
    
    // we do not have lambdas, so there should be no collision between variable names and function names
    let fib: Int = fib(a);
    println(fib(a));
    fib
}
