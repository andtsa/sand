
def test(x: Int): Int := {
    if x ≤ 0 then 1 else x * test(x - 1)
}

def main(): Int := {
    let a: Int = 9;
    let x: Int = {
        let y: Int = 4;
        a = a + y;
        let z: Int = 3;
        y * z / a
    };
aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
    let f: Bool = 5 * 4 / a;
    
    let aa: Bool = 5 * 4 / a;

    println(f);

    let ff: Int = 13;

    test(ff)
}
