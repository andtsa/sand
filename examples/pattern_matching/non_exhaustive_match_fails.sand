//@TEST: fail
// stage: typecheck
// message: includes "is not exhaustive"

// Exhaustiveness: `match` must cover every variant (or use a wildcard); here `C`
// is uncovered.

type E = A | B | C

def f(e: E): Int := match e {
    E#A => 1,
    E#B => 2,
}

def main(): Int := f(E#A)
