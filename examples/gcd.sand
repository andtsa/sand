def gcd(a: Int, b: Int): Int :=
{
    let x: Int = a;
    let y: Int = b;

    while (y != 0) do {
        let t: Int = y;
        y = x - (x / y) * y;
        x = t;
    };

    x
}

def main(): Int := {
    gcd(9, 1)
}

